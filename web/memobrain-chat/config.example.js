window.MEMOBRAIN_CHAT_CONFIG = {
  // Public HTTPS URL copied from the Dify app's published Web App.
  difyWebAppUrl: "https://your-dify.example.com/chat/replace-me",

  // Optional AdSense settings. Keep blank until the web page/site is ready
  // for AdSense. These values live on the web server, not in the APK.
  adsenseClient: "",
  adsenseSlot: "",

  // Useful during layout testing so the exact ad location is visible even
  // before AdSense is configured. Set false in production if no ad is used.
  showAdPlaceholder: true
};
