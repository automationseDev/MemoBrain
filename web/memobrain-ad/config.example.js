window.MEMOBRAIN_AD_CONFIG = {
  // These values belong only to the deployed publisher-owned web page.
  // Do not add a Dify URL here; each user's Dify URL stays on their device.
  adsenseClient: "",
  adsenseSlot: "",

  // Set true while verifying the two-WebView layout without live ads.
  showAdPlaceholder: true
};
